import{ApiError,assertDatabase,cleanText,handleError,json,readJson,requestId}from"../../_lib/api.js";import{requireAdmin,validAdminStatus}from"../../_lib/admin.js";import{assertSameOrigin}from"../../_lib/auth.js";
const load=async db=>{const result=await db.prepare("SELECT r.id,r.name,r.email,r.company,r.note,r.status,r.created_at,c.title AS course_title,c.slug AS course_slug FROM enrollment_requests r JOIN courses c ON c.id=r.course_id ORDER BY r.created_at DESC LIMIT 200").all();return result.results||[];};
export const onRequestGet=async({request,env})=>{const traceId=requestId(request);try{const db=assertDatabase(env);await requireAdmin(db,request);return json({ok:true,requests:await load(db),requestId:traceId});}catch(error){return handleError(error,traceId);}};
export const onRequestPatch=async({request,env})=>{const traceId=requestId(request);try{assertSameOrigin(request);const db=assertDatabase(env),admin=await requireAdmin(db,request),body=await readJson(request,4096),id=cleanText(body.id,80),status=cleanText(body.status,40);if(!id||!validAdminStatus("request",status))throw new ApiError(422,"validation_failed","Gültige Anfrage und Status sind erforderlich.");const exists=await db.prepare("SELECT id FROM enrollment_requests WHERE id=? LIMIT 1").bind(id).first();if(!exists)throw new ApiError(404,"request_not_found","Academy-Anfrage wurde nicht gefunden.");const now=new Date().toISOString();await db.batch([db.prepare("UPDATE enrollment_requests SET status=? WHERE id=?").bind(status,id),db.prepare("INSERT INTO audit_events(id,actor_user_id,event_type,entity_type,entity_id,metadata_json,created_at) VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(),admin.user_id,"admin.enrollment_request.status","enrollment_request",id,JSON.stringify({status}),now)]);return json({ok:true,requests:await load(db),requestId:traceId});}catch(error){return handleError(error,traceId);}};
export const onRequest=()=>json({ok:false,error:{code:"method_not_allowed",message:"Methode nicht erlaubt."}},405,{allow:"GET, PATCH"});