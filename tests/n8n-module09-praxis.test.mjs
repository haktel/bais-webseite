import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("module 09 keeps deep RAG safety and evaluation practice",async()=>{const page=await readFile("academy/n8n-bootcamp/modul-09/praxiswerkstatt/index.html","utf8"),mw=await readFile("functions/academy/n8n-bootcamp/modul-09/_middleware.js","utf8");for(const term of["Top-K","Threshold","ABSTAIN","Prompt Injection","Human Approval","Golden Dataset","Cost"])assert.match(page,new RegExp(term,"i"));assert.ok((page.match(/<details>/g)||[]).length>=5);assert.ok((page.match(/https:\/\//g)||[]).length>=4);assert.match(page,/0\.08/);assert.match(page,/0\.18/);assert.match(page,/50\.000/);assert.match(mw,/praxiswerkstatt/);assert.match(mw,/HTMLRewriter/);});