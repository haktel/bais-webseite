import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("module 02 keeps real data-engineering practice workshop",async()=>{const page=await readFile("academy/n8n-bootcamp/modul-02/praxiswerkstatt/index.html","utf8"),mw=await readFile("functions/academy/n8n-bootcamp/modul-02/_middleware.js","utf8");for(const company of["Stepstone","Formula Bot","System AI","Vodafone"])assert.match(page,new RegExp(company,"i"));assert.ok((page.match(/<details>/g)||[]).length>=5);assert.ok((page.match(/https:\/\/n8n\.io\/case-studies\//g)||[]).length>=4);assert.ok((page.match(/Break\/Fix/g)||[]).length>=3);assert.match(page,/Canonical Lead/);assert.match(mw,/praxiswerkstatt/);assert.match(mw,/HTMLRewriter/);});