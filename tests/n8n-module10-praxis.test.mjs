import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("module 10 keeps deep sub-workflow architecture practice",async()=>{const page=await readFile("academy/n8n-bootcamp/modul-10/praxiswerkstatt/index.html","utf8"),mw=await readFile("functions/academy/n8n-bootcamp/modul-10/_middleware.js","utf8");for(const term of["Copy-Paste","Typed Contract","Breaking Change","Sync vs Async","Component Catalog","Caller"])assert.match(page,new RegExp(term,"i"));assert.ok((page.match(/<details>/g)||[]).length>=5);assert.ok((page.match(/https:\/\//g)||[]).length>=4);assert.match(page,/20 caller/i);assert.match(page,/internalSecret/);assert.match(page,/Deprecate/i);assert.match(mw,/praxiswerkstatt/);assert.match(mw,/HTMLRewriter/);});