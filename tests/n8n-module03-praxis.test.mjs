import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("module 03 keeps production REST and webhook practice workshop",async()=>{const page=await readFile("academy/n8n-bootcamp/modul-03/praxiswerkstatt/index.html","utf8"),mw=await readFile("functions/academy/n8n-bootcamp/modul-03/_middleware.js","utf8");for(const term of["GitHub","Stripe","Pagination","Rate Limit","Idempotency"])assert.match(page,new RegExp(term,"i"));assert.ok((page.match(/<details>/g)||[]).length>=5);assert.ok((page.match(/https:\/\//g)||[]).length>=4);assert.match(page,/429/);assert.match(page,/503/);assert.match(page,/Signature/);assert.match(mw,/praxiswerkstatt/);assert.match(mw,/HTMLRewriter/);});