import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("module 04 keeps deep authentication and OAuth2 practice",async()=>{const page=await readFile("academy/n8n-bootcamp/modul-04/praxiswerkstatt/index.html","utf8"),mw=await readFile("functions/academy/n8n-bootcamp/modul-04/_middleware.js","utf8");for(const term of["API Key","Bearer","Authorization Code","Refresh Token","401","403","Least Privilege"])assert.match(page,new RegExp(term,"i"));assert.ok((page.match(/<details>/g)||[]).length>=5);assert.ok((page.match(/https:\/\//g)||[]).length>=4);assert.match(page,/RFC 6749/);assert.match(page,/Rotation/);assert.match(mw,/praxiswerkstatt/);assert.match(mw,/HTMLRewriter/);});