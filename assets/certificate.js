const form=document.querySelector("#certificate-form"),result=document.querySelector("#certificate-result");
form?.addEventListener("submit",async event=>{event.preventDefault();const code=new FormData(form).get("code")?.toString().trim().toUpperCase();result.className="verificationResult";result.textContent="Nachweis wird geprüft …";try{const response=await fetch("/api/certificates/"+encodeURIComponent(code),{headers:{accept:"application/json"}});const data=await response.json();if(!response.ok)throw new Error(data?.error?.message||"Nachweis konnte nicht geprüft werden.");const c=data.certificate;result.className="verificationResult "+(c.status==="valid"?"success":"error");result.innerHTML="";for(const [label,value] of [["Status",c.status==="valid"?"Gültig":"Widerrufen"],["Inhaber",c.holder],["Titel",c.title],["Programm",c.course||"–"],["Ausgestellt",c.issuedAt]]){const p=document.createElement("p"),b=document.createElement("b");b.textContent=label+": ";p.append(b,document.createTextNode(value||"–"));result.append(p);}}catch(error){result.className="verificationResult error";result.textContent=error instanceof Error?error.message:"Prüfung fehlgeschlagen.";}});