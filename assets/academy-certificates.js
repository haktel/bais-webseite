const target=document.querySelector("[data-certificate-list]");
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]));
async function load(){const response=await fetch("/api/academy/certificates",{credentials:"same-origin",headers:{accept:"application/json"}});if(!response.ok)return;const data=await response.json();target.innerHTML=data.certificates.map(item=>`<article class="certificateCard"><div><div class="ey">BAIS NACHWEIS</div><h3>${esc(item.course_title||item.title)}</h3><p><strong>${esc(item.public_code)}</strong><br>Ausgestellt: ${new Intl.DateTimeFormat("de-DE",{dateStyle:"long"}).format(new Date(item.issued_at))}</p></div><div><span class="certificateState ${item.revoked_at?"revoked":"valid"}">${item.revoked_at?"Widerrufen":"Gültig"}</span><a class="courseLink" href="${esc(item.verificationUrl)}">Öffentlich prüfen →</a></div></article>`).join("")||"<p>Noch kein Nachweis ausgestellt. Nach dokumentiertem Abschluss erfolgt die Freigabe durch BAIS.</p>";}
new MutationObserver(()=>{if(!document.querySelector("[data-dashboard]")?.hidden)load();}).observe(document.querySelector("[data-dashboard]"),{attributes:true,attributeFilter:["hidden"]});
